name: Diagnostics lint
on:
  push:
    branches: ["diagnostics"]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/diagnostics.sh || true
